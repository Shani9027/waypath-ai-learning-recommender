{
  "name": "waypath-backend",
  "version": "1.0.0",
  "private": true,
  "description": "AWS Lambda foundation for Waypath learning-path generation",
  "main": "index.js",
  "scripts": {
    "check": "node --check index.js lambda/generatePath.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.900.0"
  }
}
